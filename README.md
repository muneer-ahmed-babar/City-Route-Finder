# City-Route-Finder (DSA Project)
City Route Finder: A console-based shortest path finder using graphs, where cities are nodes and roads are weighted edges. Implements Dijkstra's Algorithm &amp; BFS to compute efficient routes between cities. Demonstrates real-world applications of DSA in navigation &amp; pathfinding.
